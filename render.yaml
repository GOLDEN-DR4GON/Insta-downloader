services:
  - type: web
    name: instagram-downloader
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python api/app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
